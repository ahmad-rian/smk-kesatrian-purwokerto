<div class="space-y-6">
    <!-- Page Header -->
    <div class="flex items-center justify-between">
        <div>
            <h1 class="text-3xl font-bold text-base-content">{{ __('Password Settings') }}</h1>
            <p class="text-base-content/70 mt-1">{{ __('Update your password to keep your account secure') }}</p>
        </div>
    </div>

    <!-- Password Component -->
    <livewire:settings.password />
</div>