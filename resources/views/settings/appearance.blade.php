<div class="space-y-6">
    <!-- Page Header -->
    <div class="flex items-center justify-between">
        <div>
            <h1 class="text-3xl font-bold text-base-content">{{ __('Appearance Settings') }}</h1>
            <p class="text-base-content/70 mt-1">{{ __('Customize the appearance of the app. Automatically switch between day and night themes.') }}</p>
        </div>
    </div>

    <!-- Appearance Component -->
    <livewire:settings.appearance />
</div>