<div class="space-y-6">
    <!-- Page Header -->
    <div class="flex items-center justify-between">
        <div>
            <h1 class="text-3xl font-bold text-base-content">{{ __('Profile Settings') }}</h1>
            <p class="text-base-content/70 mt-1">{{ __('Update your name and email address') }}</p>
        </div>
    </div>

    <!-- Profile Component -->
    <livewire:settings.profile />
</div>